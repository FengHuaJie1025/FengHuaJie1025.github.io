{
  "name": "personal-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "postbuild": "pagefind --site dist"
  },
  "dependencies": {
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
